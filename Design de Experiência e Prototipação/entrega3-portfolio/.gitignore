node_modules/
dist/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/

# Export redundante gerado pelo Figma Make (mesmo codigo ja versionado em src/)
Create Futuristic Portfolio.zip

# Memoria tecnica local (handoff entre agentes) - fora do repositorio publico.
# docs/screenshots/ continua versionado: o README depende dele.
/specs/
/docs/state.md
